- args:
    permission:
      allow_aggregations: false
      backend_only: false
      columns:
      - id
      - name
      - logo
      - logo_square
      - issue_link_template
      - pr_link_template
      - project_id
      - created_at
      - updated_at
      computed_fields: []
      filter:
        members:
          employee:
            user_id:
              _eq: X-Hasura-User-Id
      limit: null
    role: user
    table:
      name: teams
      schema: public
  type: create_select_permission
