version: 2
tables:
- table:
    schema: public
    name: organization
  insert_permissions:
  - role: app_admin
    permission:
      check: {}
      columns:
      - logo
      - logo_square
      - name
      backend_only: false
  select_permissions:
  - role: app_admin
    permission:
      columns:
      - logo
      - logo_square
      - name
      - id
      filter: {}
  update_permissions:
  - role: app_admin
    permission:
      columns:
      - logo
      - logo_square
      - name
      filter: {}
      check: {}
- table:
    schema: public
    name: role
  is_enum: true
- table:
    schema: public
    name: user
