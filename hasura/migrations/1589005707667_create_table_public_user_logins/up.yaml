- args:
    cascade: false
    read_only: false
    sql: CREATE TABLE "public"."user_logins"("provider" text NOT NULL, "public_key"
      text NOT NULL, "private_key" text NOT NULL, "user_id" uuid NOT NULL, PRIMARY
      KEY ("provider","public_key") , FOREIGN KEY ("user_id") REFERENCES "public"."users"("id")
      ON UPDATE cascade ON DELETE restrict);
  type: run_sql
- args:
    name: user_logins
    schema: public
  type: add_existing_table_or_view
