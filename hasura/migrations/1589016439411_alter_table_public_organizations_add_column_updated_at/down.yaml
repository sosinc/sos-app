- args:
    cascade: false
    read_only: false
    sql: |-
      DROP TRIGGER IF EXISTS "set_public_organizations_updated_at" ON "public"."organizations";
      ALTER TABLE "public"."organizations" DROP COLUMN "updated_at";
  type: run_sql
